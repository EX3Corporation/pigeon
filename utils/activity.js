const { loadStats } = require('./stats');

function updateActivity(bot) {
  /*
  const stats = loadStats();
  let totalMessages = 0;
  for (const user in stats) {
    totalMessages += stats[user].messages;
  }
  bot.user.setActivity(`pigeon.exerinity.com - ${totalMessages.toLocaleString()} messages`, { type: "CUSTOM" });*/
  null;
}

module.exports = { updateActivity };
